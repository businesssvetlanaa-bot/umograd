{
  "name": "umograd",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd client && npm ci --include=dev && npm run build && cd ../server && npm ci --include=dev && npx prisma generate && npm run build",
    "start": "cd server && npm run db:init:local && npm run db:seed && node dist/index.js"
  }
}
